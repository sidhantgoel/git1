SELECT aforce_roster.*, appsmith_team.name, appsmith_team.email, appsmith_team.slack_id FROM aforce_roster inner join appsmith_team on aforce_roster.hero_id = appsmith_team.id WHERE start_date >= {{moment().startOf("week").add(1, "days")}} ORDER BY start_date asc LIMIT 200;